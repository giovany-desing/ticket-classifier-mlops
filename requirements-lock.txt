# Requirements con versiones fijas para reproducibilidad
# 
# IMPORTANTE: Este archivo contiene versiones fijas de las dependencias CRÍTICAS
# para garantizar reproducibilidad. Las demás dependencias se instalan desde
# requirements.txt para mantener compatibilidad.
#
# Para generar un lock file completo:
#   pip install -r requirements.txt
#   pip freeze > requirements-lock-full.txt
#
# Para setup reproducible:
#   pip install -r requirements-lock.txt  # Dependencias críticas con versiones
#   pip install -r requirements.txt       # Resto de dependencias

# ============================================================================
# DEPENDENCIAS CRÍTICAS CON VERSIONES FIJAS
# ============================================================================

# Core ML Libraries (críticas para reproducibilidad de modelos)
numpy==1.24.3
pandas==2.0.3
scikit-learn==1.3.0
scipy==1.11.1
joblib==1.3.2

# ML Frameworks (afectan resultados de entrenamiento)
xgboost==2.0.2
lightgbm==4.1.0
optuna==3.3.0
optuna-integration==3.3.0

# MLflow (tracking de experimentos)
mlflow==2.8.1
mlflow-skinny==2.8.1

# NLP (afecta preprocesamiento)
nltk==3.8.1

# API Framework
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Data Version Control
dvc[s3]==3.38.1
dvc-data==2.10.0
dvc-s3==3.1.0

# AWS (para DVC)
boto3==1.29.7
botocore==1.32.7
s3fs==2023.12.2

# Utilities esenciales
requests==2.31.0
PyYAML==6.0.1
python-dotenv==1.0.0

# Database
supabase==2.0.0

# ============================================================================
# NOTA: Las demás dependencias se instalan desde requirements.txt
# Esto mantiene compatibilidad mientras garantiza reproducibilidad de
# los componentes críticos (ML, preprocesamiento, etc.)
# ============================================================================

